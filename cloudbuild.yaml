steps:
  - name: gcr.io/cloud-builders/docker
    args:
      ["build", "-t", "gcr.io/demo/preprocess:${SHORT_SHA}", "--target","preprocess","."]
    id: Build-preprocess
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/demo/preprocess"]
    id: Push-preprocess
